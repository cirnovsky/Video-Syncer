## ‼️ Things to Notice

1. Frame filenames should be sorted by alphabetical order.

2. `converter.py` is used to extrat frames from a video. If the extraction has been beforehand, it could be ignored.

3. Run `python3 syncer.py -h` to see the description for the list of patameters.